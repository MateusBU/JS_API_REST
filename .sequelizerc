// Sequelize is a powerful library in Javascript that makes it easy to manage a SQL database

const {resolve} = require('path');

//caminhos para cada arquivo
module.exports = {
    config: resolve(__dirname, 'src','config','database.js'),
    'models-path': resolve(__dirname,'src','models'),
    'migrations-path': resolve(__dirname,'src','database','migrations'), //migrations é relacionado a base de dados
    'seeders-path': resolve(__dirname,'src','database','seeds'),
};

